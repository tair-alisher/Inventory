@model Inventory.Web.Models.ComponentTypeVM

@{
    ViewBag.Title = "Тип комплектующего";
}

<div class="block">
    <h2 class="block-title">Просмотр</h2>

    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

        </dl>
    </div>
    <div class="form-group">
        <div class="btn-group" role="group">
            <a href="@Url.Action("Edit", "ComponentType", new { id = Model.Id})" class="btn btn-success"><i class="fa fa-pencil" aria-hidden="true" title="Редактировать" data-toggle="tooltip" data-placement="top"></i> Редактировать</a>
            <a href="@Url.Action("Index", "ComponentType")" class="btn btn-info"><i class="fa fa-arrow-left" aria-hidden="true" title="Назад" data-toggle="tooltip" data-placement="top"></i> Назад</a>
        </div>
    </div>

</div>